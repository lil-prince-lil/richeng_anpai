# 语音速记日程 App —— 产品与技术方案（v1）

> 一句话定位：**按一下、说一句话，AI 自动把它变成一条清晰的日程。**
> 目标用户：日程多、又怕麻烦、不爱手动记录的人（也就是你本人）。
> 平台优先级：**iPhone 优先**。
> 文档状态：方案蓝图，作为后续开发依据。

---

## 一、产品背景与核心痛点

你的真实场景：
- 每天有很多人通过电话、微信、当面约你——「哪天有空见一面」「X 号方便吗」。
- 事情多、节奏快，打完电话/聊完天，脑子记了，但没真正落到日程表里，最后忘掉。
- 现有的日历/待办 App 都要打字、要点好几下，太麻烦，所以你根本不愿意用。

核心结论：**问题不在「没有日程工具」，而在「记录的摩擦太大」。**
所以这个产品的第一性原理只有一句话：

> **把「记一条日程」的成本压到接近于零——最好只是按一下、说一句话。**

---

## 二、关于你提到的几个想法的可行性判断

你提了几个方向，这里诚实地分级，避免在做不成的方向上浪费时间。

| 想法 | 可行性 | 结论 |
|---|---|---|
| **① 一句话语音 → AI 解析成日程** | ✅ 完全可行，体验可做到极佳 | **核心刚需，第一版就做** |
| **② iPhone 物理按钮/快捷键秒级触发** | ✅ 可行（操作按钮 / 快捷指令 / 锁屏 / Watch / Siri） | **和 ① 一起做** |
| **③ 24 小时常驻监听麦克风/对话** | ⚠️ 技术能写，但 iOS 严禁后台长期占用麦克风，会被杀进程、极度耗电、几乎必被 App Store 拒审，且隐私风险极高 | **不做。用「极速触发」替代「常驻偷听」** |
| **④ 录屏/监控微信屏幕抓聊天** | ⚠️ iOS 沙盒禁止读取其他 App 屏幕内容，做不了也过不了审 | **不做录屏。用「分享扩展」替代** |
| **⑤ 微信聊天接入** | ✅ 可行（用 iOS 分享扩展，选中文字 → 分享给本 App → AI 解析） | **第二版做，合规且顺手** |

### 为什么不做「24 小时监听」
- iOS 不允许普通 App 长期后台录音；即便用各种技巧绕过，也会被系统终止、耗电爆炸。
- App Store 审核基本会直接拒。
- 隐私上会录到所有人、所有对话，法律和信任风险极大。
- **替代方案**：把「触发」做到一秒级（操作按钮/Siri/锁屏小组件），体验上就接近「随口一说」，但不需要常驻偷听。行业里所有同类产品都是这么做的。

### 为什么不做「微信录屏」
- iOS 应用之间是隔离的，你的 App 读不到微信的屏幕文字；ReplayKit 录屏也读不到内容、且必须用户手动发起。
- **替代方案**：iOS **分享扩展（Share Extension）**。在微信里长按选中对方那句话 →「分享」→ 选你的 App → AI 直接解析成日程。合规、稳定、顺手。

---

## 三、产品路线图

分三期，每一期都能独立上线、独立产生价值。

### MVP（第一版）—— 本文档的开发重点
- 语音速记：触发 → 说一句话 → 语音转文字 → AI 解析 → 自动生成日程。
- 极速触发：操作按钮 / 快捷指令 / 锁屏小组件 / Apple Watch / 「嘿 Siri」。
- 清爽视图：今天 / 接下来几天的列表 + 日历视图。
- 写入系统日历（可选）：让日程也出现在 iOS 自带日历里，并能弹提醒。

### 第二版
- 微信/聊天场景接入：分享扩展，选中文字 → AI 解析（不碰录屏）。
- 自然语言改期/取消：「把和张总那个挪到周五下午」。
- 多设备同步（iCloud）。

### 第三版
- 智能提醒、冲突检测（同一时间段两个安排时提示）。
- 联系人/地点识别与聚合（「这周和谁见面」）。
- 周/月视图、统计。

---

## 四、MVP 用户流程（端到端）

```
[触发] 按操作按钮 / 长按锁屏小组件 / 抬腕对 Watch 说 / "嘿 Siri 记一下"
   ↓
[录音] App 弹出极简录音界面（或无界面，直接录）
   ↓ 说："下周三下午三点，在公司见一下张总，聊合同"
[转文字] iOS 本地语音识别 → "下周三下午三点 在公司 见张总 聊合同"
   ↓
[AI 解析] 调用大模型 → 结构化 JSON：
   {
     title: "见张总聊合同",
     person: "张总",
     location: "公司",
     startTime: "2026-06-24T15:00:00",
     endTime: null,
     note: "聊合同"
   }
   ↓
[确认] 一屏展示解析结果，可一键确认 / 轻点修改时间
   ↓
[入库] 存本地 + （可选）写入系统日历 + 设提醒
   ↓
[查看] 首页「今天 / 接下来」一目了然
```

设计原则：**默认相信 AI 的解析，确认页只在「时间不确定/有歧义」时才需要用户介入**，其余情况一键确认甚至自动入库。

---

## 五、技术架构（iPhone 优先）

### 5.1 总览

```
┌─────────────────────────── iOS App (SwiftUI) ───────────────────────────┐
│  触发层    操作按钮 / App Intents(快捷指令/Siri) / 锁屏小组件 / Watch App   │
│  录音层    AVAudioEngine 录音                                             │
│  识别层    Speech.framework (SFSpeechRecognizer，端侧识别，免费)           │
│  解析层    调用后端 /parse 接口（保护 API Key）                            │
│  存储层    SwiftData(本地) + CloudKit(同步, 第二版) + EventKit(写系统日历)  │
│  视图层    今日/未来列表 + 日历视图 + 确认页                                │
└───────────────────────────────────┬──────────────────────────────────────┘
                                     │ HTTPS (只传文字，不传音频)
                                     ▼
┌──────────────────── 轻量后端 (Cloudflare Workers / Vercel / 一台小服务器) ─┐
│  POST /parse   { text, now, timezone }                                    │
│     → 调用 Claude API 做结构化抽取                                          │
│     → 返回 { entries: [ {title, person, location, startTime, ...} ] }      │
│  作用：① 保护 Anthropic API Key 不进客户端 ② 统一 prompt/模型，便于迭代      │
└────────────────────────────────────────────────────────────────────────┘
```

### 5.2 关键技术选型

| 模块 | 选型 | 理由 |
|---|---|---|
| App 框架 | **SwiftUI**（iOS 17+） | 原生、支持小组件/Live Activity/Watch |
| 极速触发 | **App Intents** + 操作按钮 + 锁屏小组件 | iOS 原生「一句话/一按」入口；iPhone 15 Pro+ 有物理操作按钮 |
| 语音转文字 | **Speech.framework（端侧）** | 免费、快、隐私好、可离线；中文识别够用。后续要更准可换 Whisper |
| 日程解析 | **Claude API（见 5.3）** | 中文语义解析强，结构化输出稳定 |
| 本地存储 | **SwiftData** | 苹果原生持久化，简单 |
| 云同步 | **CloudKit**（第二版） | 免服务器、跟着 Apple ID 走 |
| 系统日历 | **EventKit** | 写入 iOS 日历 + 提醒，复用系统能力 |
| 后端 | **Cloudflare Workers / Vercel Functions** | 轻量、便宜、主要用于代理大模型调用 |

> **为什么要一个后端？** 因为 Anthropic 的 API Key 绝不能打包进 iOS App（会被人扒出来盗刷）。后端只做一件事：接收文字、调用大模型、返回结构化结果。它也让你能随时改 prompt、换模型，而不用重新发版。

### 5.3 大模型解析方案（Claude API）

用 Claude 做「自然语言 → 结构化日程」的抽取。这是典型的**单次调用 + 结构化输出**任务。

- **默认模型**：`claude-opus-4-8`（解析最准、最稳）。
- **成本优化备选**：`claude-haiku-4-5`（$1/$5 每百万 token，速度快、便宜）。日程解析是短输入短输出的简单任务，**实际生产很可能用 Haiku 就足够**——建议上线后用真实语料 A/B 一下：Haiku 解析准确率达标就用 Haiku，省钱省延迟；不达标再上 Opus。这是成本/质量取舍，由你后续按数据决定。
- **关键技巧**：用**结构化输出（Structured Outputs / `output_config.format`）** 强制模型返回固定 JSON schema，避免解析失败。
- **传入当前时间和时区**：模型必须知道「现在是几月几号、周几」，才能把「下周三」「明天」算成绝对时间。

后端 `/parse` 的核心调用（Python 示例，正式实现按后端语言调整）：

```python
import anthropic

client = anthropic.Anthropic()  # API Key 从环境变量读取，绝不硬编码

SYSTEM = """你是一个日程解析助手。用户会用口语描述一件或多件待办/约会。
请把它解析成结构化日程。规则：
- 依据传入的"当前时间"把相对时间（明天、下周三、下午三点）换算成绝对时间。
- 时间不明确时，startTime 留空并在 needsConfirm 标记 true。
- 一句话里可能包含多件事，拆成多条。
- 只输出 schema 规定的字段，不要编造信息。"""

SCHEMA = {
    "type": "object",
    "properties": {
        "entries": {
            "type": "array",
            "items": {
                "type": "object",
                "properties": {
                    "title":      {"type": "string"},
                    "person":     {"type": ["string", "null"]},
                    "location":   {"type": ["string", "null"]},
                    "startTime":  {"type": ["string", "null"], "format": "date-time"},
                    "endTime":    {"type": ["string", "null"], "format": "date-time"},
                    "note":       {"type": ["string", "null"]},
                    "needsConfirm": {"type": "boolean"}
                },
                "required": ["title", "needsConfirm"],
                "additionalProperties": False
            }
        }
    },
    "required": ["entries"],
    "additionalProperties": False
}

def parse_schedule(text: str, now_iso: str, tz: str):
    resp = client.messages.create(
        model="claude-opus-4-8",   # 成本敏感可切换为 claude-haiku-4-5
        max_tokens=2000,
        system=SYSTEM,
        messages=[{
            "role": "user",
            "content": f"当前时间：{now_iso}（时区 {tz}）。\n用户说：{text}"
        }],
        output_config={"format": {"type": "json_schema", "schema": SCHEMA}},
    )
    text_block = next(b.text for b in resp.content if b.type == "text")
    return text_block  # 已是符合 schema 的 JSON 字符串
```

> 模型 ID 以本文档为准：`claude-opus-4-8`（默认）、`claude-haiku-4-5`（成本备选）。不要给 ID 加日期后缀。

### 5.4 数据模型（本地 SwiftData）

```swift
@Model
final class ScheduleEntry {
    var id: UUID
    var title: String          // 事项，如「见张总聊合同」
    var person: String?        // 涉及的人
    var location: String?      // 地点
    var startTime: Date?       // 开始时间（可空 = 待确认）
    var endTime: Date?         // 结束时间
    var note: String?          // 备注/原话
    var rawText: String        // 语音原始转写，便于回溯
    var source: String         // 来源：voice / wechat / manual
    var status: String         // pending(待确认) / confirmed / done / cancelled
    var calendarEventID: String?   // 若已写入系统日历，存 EventKit 的 id
    var createdAt: Date
}
```

---

## 六、极速触发方案（iPhone 的核心体验）

目标：**用户从「想记」到「说完」不超过 2~3 秒，且不用打字。**

| 触发方式 | 适用机型 | 体验 |
|---|---|---|
| **操作按钮（Action Button）** | iPhone 15 Pro 及以上 | 按一下侧边物理键 → 直接进录音，最快 |
| **快捷指令 + Siri** | 全部 iPhone | 「嘿 Siri，记个日程」→ 说一句话 |
| **锁屏/桌面小组件** | 全部 iPhone | 锁屏点一下图标直接录音，不用解锁进 App |
| **Apple Watch App** | 有 Watch 的用户 | 抬腕说话，连手机都不用掏 |
| **App 内大按钮** | 全部 | 兜底，打开 App 就是一个大大的「按住说话」 |

这些都通过 **App Intents** 框架实现，一套意图（Intent）可同时被操作按钮、快捷指令、Siri、小组件复用。

---

## 七、隐私与合规（重要）

- **音频不出本机**：语音识别用 iOS 端侧 Speech.framework，只把**识别后的文字**发给后端解析，音频本身不上传。
- **不做后台监听、不做录屏**：从产品上规避最大的隐私与审核风险。
- **明确授权**：首次使用申请麦克风、语音识别、日历权限，并说明用途。
- **数据最小化**：后端只处理文字、不长期存储用户日程内容（解析完即返回）。
- App Store 审核友好：以上设计基本规避了语音类 App 最常见的拒审点。

---

## 八、里程碑与开发顺序

建议的搭建顺序（每步都可独立验证）：

1. **后端 `/parse` 接口** —— 先把「文字 → 结构化日程」跑通（可先用 curl 测）。
2. **iOS App 骨架** —— SwiftUI 工程 + SwiftData 数据模型 + 今日列表视图。
3. **录音 + 端侧识别** —— 录一句话，转成文字，调 `/parse`，展示确认页。
4. **极速触发** —— App Intents 接入操作按钮 / 快捷指令 / Siri。
5. **写入系统日历 + 提醒** —— EventKit 集成。
6. **小组件 / Watch** —— 锁屏小组件、Watch App（可选增强）。

---

## 九、待你后续拍板的点（不影响现在开工）

- 后端用什么语言/平台（Cloudflare Workers 用 TS，还是一台小服务器用 Python）。
- 解析模型最终用 Opus 还是 Haiku（上线后用真实语料测准确率再定）。
- 是否第一版就写入系统日历，还是先只在 App 内显示。

---

*本文件为 v1 方案蓝图。下一步可按「第八节」的顺序，从后端 `/parse` 接口或 iOS 骨架开始搭建。*
